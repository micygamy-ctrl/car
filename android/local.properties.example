# ============================================================
# هذا ملف توضيحي فقط (Template) — انسخ القيم لملف
# android/local.properties (بدون .example) وحط مفتاحك الحقيقي فيه.
# ملف local.properties الحقيقي مستثنى من Git بالفعل (.gitignore)
# فمش هيترفع على GitHub أبدًا.
# ============================================================

# مفتاح Google Maps (يُستخدم داخل AndroidManifest.xml لعرض الخريطة نفسها)
MAPS_API_KEY=ضع_مفتاح_خرائط_جوجل_هنا

# (الإعدادات الافتراضية لـ Flutter SDK تُضاف هنا تلقائيًا من Flutter،
#  لا تحذفها لو كانت موجودة عند أول تشغيل للمشروع)
